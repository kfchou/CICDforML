"""This is an example module: a group of related functions for use in scripts etc."""


def add_one(value: int) -> int:
    """Returns the input value plus 1.

    Args:
        value: the value which you want to add 1 to

    Returns:
        output: the value plus 1
    """
    return value + 1
